<form method="post" action="{{ path('registro_legajo_delete', {'id': legajo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ legajo.id) }}">
    <button class="btn">Delete</button>
</form>
